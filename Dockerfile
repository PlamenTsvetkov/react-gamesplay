# Използвайте образ на Node.js като базов образ
FROM node:17-alpine

# Установете рабочата директория в контейнера
WORKDIR /app

# Копирайте package.json и package-lock.json в контейнера
COPY package.json .

# Инсталирайте зависимостите
RUN npm install

# Копирайте останалите файлове на приложението в контейнера
COPY . .

# Изложете порта, който ще използвате за вашия JSON-сървър (например 3000)
EXPOSE 3000

# Стартирайте JSON-сървъра и React приложението
CMD ["npm", "start"]
